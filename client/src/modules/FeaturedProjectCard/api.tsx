export interface FeaturedProjectCardStyles {
  links: {
    container: string;
    span: string;
    svg: string;
  };
  project: {
    styles: string;
    title: string;
    description: string;
  };
}
