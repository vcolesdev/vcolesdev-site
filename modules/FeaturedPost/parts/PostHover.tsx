import styles from "@/modules/FeaturedPost/styles.module";

export default function PostHover() {
  const hoverElementClasses = styles.hoverElement;
  return <div className={hoverElementClasses} />;
}