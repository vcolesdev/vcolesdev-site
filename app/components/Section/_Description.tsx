export default function SectionDescription({textContent}: {textContent: string}) {
  return (
    <p className="mt-6 pr-8 text-xl tracking-tight leading-8 text-rosy_brown-200/60">
      {textContent}
    </p>
  );
}